package me.rerere.rikkahub.data.db.converter

import androidx.room.TypeConverter

/**
 * 表格模板类型转换器（简化版本）
 */
object TableTemplateConverter {
    // 由于TableTemplate使用String columnsJson，不需要复杂的类型转换
    // 保留此文件以避免AppDatabase中的TypeConverters引用错误
}